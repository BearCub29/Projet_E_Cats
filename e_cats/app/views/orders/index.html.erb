<div class="container">
  <div class="card col p-0 m-1" >
    <div class="card-header">
      <h3><i class="fas fa-box-open"></i> Commandes</h3>
        </div>
        <ul class="list-group list-group-flush">
        <% @orders.each do |order| %>
          <li class="list-group-item"> <i class="fas fa-box-open"></i><p> Commande n° : <%= order.id %></p><p> Date :<%= order.created_at.strftime("%F | %H:%M ") %></p>
          </li>
          </ul>
          <% end %>
  </div>
</div>


