<% x = 1 %>
<% if x == 1 %>

<div class="container">
  <div class="card col p-0 m-1" >
    <div class="card-header">
      <h3><i class="fas fa-box-open"></i> All My Orders: </h3>
        </div>
        <ul class="list-group list-group-flush">
        <% @joins.each do |join| %>
          <li class="list-group-item"> 
            <p><span class="text-muted"> n° order : </span><%= @item =  Item.find(join.item_id).id %> </p>
            <p><span class="text-muted"> date order: </span><%= Item.find(join.item_id).created_at %> </p>
            <p> <span class="text-muted"> articles: </span><%= Item.find(join.item_id) %> </p>
          </li>
          </ul>
          <% end %>

  </div>
</div>

<% end %>